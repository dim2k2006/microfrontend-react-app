{
  "name": "users-repository",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/users-repository/src",
  "projectType": "library",
  "tags": ["scope:users-repository"],
  "// targets": "to see all targets run: nx show project users-repository --web",
  "targets": {
    "prettier": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "npx prettier --check 'libs/users-repository/**/*.{ts,tsx}'"
      }
    },
    "tsCheck": {
      "executor": "./tools/executors/tsCheck:tsCheck"
    }
  }
}
