{
  "name": "users-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/users-service/src",
  "projectType": "library",
  "tags": ["scope:users-service"],
  "// targets": "to see all targets run: nx show project users-service --web",
  "targets": {
    "prettier": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "npx prettier --check 'libs/users-service/**/*.{ts,tsx}'"
      }
    },
    "tsCheck": {
      "executor": "./tools/executors/tsCheck:tsCheck"
    }
  }
}
