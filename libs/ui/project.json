{
  "name": "ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ui/src",
  "projectType": "library",
  "tags": ["scope:ui"],
  "// targets": "to see all targets run: nx show project ui --web",
  "targets": {
    "prettier": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "npx prettier --check 'libs/ui/**/*.{ts,tsx}'"
      }
    }
  }
}
